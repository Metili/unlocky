import 'package:flutter/material.dart';

// ignore: must_be_immutable
class Cadenas extends StatelessWidget {
  const Cadenas({super.key});


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Cadenas page',
      ),
    );
  }
}
